import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.mtg_e_absensi',
  appName: 'MTG e-Absensi',
  webDir: 'dist',
  server: {
    androidScheme: 'http'
  }
};

export default config;
